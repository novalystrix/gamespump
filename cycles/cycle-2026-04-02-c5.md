# Cycle Report — 2026-04-02 (Cycle 5)

## Build Track
- **Agent**: Nova
- **Concern**: Bug fixes / polish (🟡 MED)
- **Changes**: 
  - maxPlayers enforcement in `joinRoom()` — rooms reject joins when full, with clean 400 "Room is full" vs 404 differentiation. Rejoin (same player ID) always allowed.
  - Disconnected player skip in Quick Draw & Lie Detector — while-loop skips turns of players who left, falls through to leaderboard if no valid players remain.
  - i18n for hardcoded "Starting..." string — replaced in all 10 games with `t('common.starting')`, Hebrew "מתחיל..." added.
  - Pulse animation on "Waiting for host" text for non-host players on leaderboard.
- **Status**: ✅ Shipped (commit 4c40f35, deployed to Render)
- **Next**: Client-side "(player left)" visual indicator for turn-based games (carry-forward from Orion's QA). Sound/haptics polish.

## Growth Track
- **Agent**: Orion
- **Concern**: Track Usage (w3)
- **Changes**:
  - `POST /api/analytics/events` — validated batch receiver with event whitelist, IP hashing (SHA-256), rate limiting, payload size cap.
  - `GET /api/analytics/dashboard` — cross-user aggregated stats from PostgreSQL (visitors, sessions, game starts/completions, return rate, drop-offs).
  - `AnalyticsProvider` component — auto-flushes events every 60s + sendBeacon on page close.
  - `db.ts` with pg Pool, `analytics-schema.sql` for table auto-creation.
- **Status**: ✅ Shipped (PR #1, cherry-picked as commit 410a913)
- **Next**: Set `DATABASE_URL` env var on Render to go live. Then Data-Driven Improvement (w1) becomes actionable.

## Notes
- Orion's PR had conflicts with i18n changes (his fork was pre-i18n). Resolved by cherry-picking + aligning field names (type/data vs event/props).
- Fixed AnalyticsEvent export, added missing getEvents() helper, and used COALESCE in dashboard queries for gameType/gameId compatibility.
- Minor carry-forward items: rate limiter memory leak cleanup, batch INSERT optimization, visibilitychange listener cleanup bug in AnalyticsProvider.
- All 🟡 MED build concerns now addressed. Remaining: 🟢 LOW items only (sound/haptics, accessibility, QR codes, social media).
